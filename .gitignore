# Internal business/strategy notes — keep private, do NOT publish
PLAN.md
MEMORY.md
CLAUDE.md
PROJECT.md

# Personal analytics tool + marketing drafts — keep private
stats.ps1
stats-history.csv
marketing/

# Build output (installer is shipped via GitHub Releases, not committed)
Output/

# Bundled binary runtime — downloaded separately (GPL; provide via Releases / build step)
src/AutoHotkey64.exe
*.exe

# Inno Setup build leftovers
*.iss.bak

# Generated at compile time by SnapPastePro.iss (from MyAppVersion) — never edit by hand
src/version.txt

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
.vs/
.idea/
*.swp
